@model IEnumerable<MyForum.BL.Entities.Repl>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Picname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Picname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comment.CommentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRepl">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRepl">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRepl">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
